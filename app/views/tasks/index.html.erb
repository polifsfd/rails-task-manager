<h1>Task List</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task.title %>
      (<%= task.completed ? 'Completed' : 'Incomplete' %>)
      <p><%= task.details %></p>
    </li>
  <% end %>
</ul>
